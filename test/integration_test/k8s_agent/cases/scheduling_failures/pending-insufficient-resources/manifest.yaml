apiVersion: v1
kind: Pod
metadata:
  name: sample-app
  labels:
    app: sample-app
spec:
  containers:
    - name: app
      image: nginx:latest
      resources:
        requests:
          cpu: "100"
          memory: "4Gi"
