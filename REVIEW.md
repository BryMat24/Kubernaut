1. tools such as
   cpu_saturation
   error_logs
   recent_logs
   latency_95
   don't what to return

2. git open PR error
   Failed to open PR: command failed: git push -u origin agent/fix-the-cache-service-connectivity-in-th-06e848
   fatal: could not read Username for 'https://github.com': No such device or address

3. ai struggle to answer just informational questions, evaluation verdict always gate it since it doesn't follow playbook,
   how to route this till the end
   (always get Investigation did not reach a confident root cause within the hypothesis budget (3 hypotheses). Escalating to human review rather than risking a false negative.
   when asked how many deployments in dev namespace)
