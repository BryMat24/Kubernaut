[pytest]
markers =
    integration: real integration tests against a live cluster and real LLM calls; excluded by default, run explicitly with `-m integration`
addopts = -m "not integration"
log_cli = true
log_cli_level = INFO
