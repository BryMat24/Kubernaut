apiVersion: v1
kind: Service
metadata:
  name: kubernaut-api
  labels:
    app.kubernetes.io/name: kubernaut
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kubernaut
    app.kubernetes.io/component: api
  ports:
    - port: 8000
      targetPort: 8000
