apiVersion: v1
kind: Service
metadata:
  name: kubernaut-ui
  labels:
    app.kubernetes.io/name: kubernaut
    app.kubernetes.io/component: ui
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kubernaut
    app.kubernetes.io/component: ui
  ports:
    - port: 3000
      targetPort: 3000
